export const imageSize = {
    width: 378,
    height: 363,
    ratio: 363 / 378,
};

export const domSize = {
    width: imageSize.width * 0.15 * window.innerHeight / imageSize.height,
    height: 0.15 * window.innerHeight,
};

export const leftArrowSize = {
    width: 15,
    height: 60,
    ratio: 4,
};

export const rightArrowSize = {
    width: 15,
    height: 54,
    ratio: 18 / 5,
};
